# Strauss PDE solutions manual。主書 num 為三層 N.M.K，problem_re group(1) 逐字對齊。
#
# === 2026-06-23 解鎖（accumulate 重複章錨，commit c1368d8）===
# 原阻塞：`Chapter N` 被當 running header 重複灌入（ch3×15 / ch5×16 / ch9×21…，14 章 133 anchor），
# 舊引擎 `out[ch_num]=problems` 覆蓋非累積，每章只留最後一個 running-header 視窗 → dry-run 僅 24/651。
# 修法：extract_sol_chapters 改 `out.setdefault(ch).update()` 累積同章多區間 → dry-run 279/651（42.9%）。
# 殘缺 = solutions manual 部分覆蓋（source 真實上限，非錯位）。語義抽樣 1.1.2/1.2.1/5.1.2/5.2.1/10.1.2
# 題幹與解答逐字同題。提案 P-2026-06-19-strauss-pde-sol → superseded。
chapter_re: '^Chapter\s+(\d+)\s*$'
problem_re: '^(\d+\.\d+\.\d+)\.'
multi_per_block: false
