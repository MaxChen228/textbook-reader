# Hennessy & Patterson CAQA 6e solutions manual
#
# 現況判定（2026-06-20）：
# 1. 正式 dry-run（預設規則）穩定抽出 0 章、0 題。
# 2. unified/content_list.json 內可辨識章標只有：
#      - idx 0   = "Chapter 1 Solutions"
#      - idx 228 = "Chapter 3 Solutions"
#      - idx 349 = "Chapter 4 Solutions"
#      - idx 429 = "Chapter 5 Solutions"
#      - idx 769 = "Chapter 6 Solutions"
#    它們全部都是 text_level=2；chapter 2 / 7 章標缺失。
# 3. 現行 sol_extract 只接受 text_level==1 的 chapter anchor，因此即使 chapter_re
#    與 problem_re 本身合理，也無法用正式流程切章 merge。
# 4. 問題鍵值本身大致正確：以 problem-only 探針可抽出 166 題（1.1..7.12），
#    抽樣 1.1 / 2.1 / 3.1 / 5.1 / 6.1 與主書題幹語義對齊，排除版次錯書。
# 5. source 也有殘缺：未見 4.1、4.3，且 chapter 1 / 4 / 7 都有缺題，但若引擎能吃
#    非 lvl1 章標或直接依題號前綴分章，仍可做部分高品質 merge。
#
# 結論：目前阻塞是 harness-gap，不是 edition mismatch。保持 _pending，等待
# sol_extract 支援 non-lvl1 chapter anchor 或 chapterless / key-prefix fallback。
chapter_re: '^Chapter\s+(\d+)\s+Solutions$'
problem_re: '^(\d+\.\d+[a-z]?)\b'
multi_per_block: false
# ── 2026-06-22 章錨層級可配（chapter_level，commit 50fdc37）解鎖 ──────────────
# 原 _pending 因「sol_extract 章錨硬綁 text_level==1」而設，該 harness gap 已關閉。
# 複驗：dry-run 配對 78% (109/139)；語義抽樣 yield/PTX/WSC-TCO 對齊；殘缺為未解題。
# → 移除 _pending，放行 daemon merge（proposal 已 superseded）。
