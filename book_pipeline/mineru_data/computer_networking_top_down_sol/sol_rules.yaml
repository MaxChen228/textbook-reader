# ⚠ PENDING — 不要 merge。殘留阻塞 = 部分章「版次錯位」（edition），非引擎缺能力。
#
# === 2026-06-23 更新（num_template 能力已落地，commit c1368d8）===
# 原【ENGINE-GAP，key prefix 模板】已解決：sol 裸題標 `Problem 1`，主書 num 帶固定 `P` 前綴 `P1`
# （本書只收 Problems，全 P 前綴）。引擎已加 `num_template: 'P{}'`，把 problem_re group(1) 套進模板
# 對齊主書 key。in-process 驗證：除版次錯位章外題號完全對齊。下方已設 num_template，配置就緒。
#
# 唯一殘留【部分章版次錯位，引擎無解、需 re-source】：8th edition 主書 vs 本 sol 在 ch4/ch7 章內
# 題目編排不同版本，同題號配到不同題（ch7 整章 13 題系統性不符：主書 P1 "Wireless speeds" ↔ sol
# CDMA 輸出；ch4 約半錯位：P2 ↔ distance-vector）。缺 ch5（sol 無 Chapter 5 章錨）。
# 對齊正確章（語義抽樣確認）：ch1/ch2/ch3/ch6/ch8。
#
# 結論：正確終態仍 = _pending。若直接 merge，ch4/ch7 會塞錯解答（違鐵律）。
# 架構師補救＝ re-source 版次對齊的 sol（/restock 域），或可選 skip_chapters 排除 ch4/ch7 只 merge
# 對齊章——但版次錯位本質是源頭問題，依 SOP「edition → 續 proposed、不硬幹」續 _pending。
# 提案 P-2026-06-19-computer-networking-top-down-sol 續 proposed（殘留已從 harness-gap 收斂為 edition）。
chapter_re: '^Chapter\s+(\d+)\s+Problems'
problem_re: '^Problem\s+(\d+)\b'
num_template: 'P{}'
multi_per_block: false
chapter_level: 2
_pending: true
